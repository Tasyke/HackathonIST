<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:HackathonIST"
             x:Class="HackathonIST.Registration">
    <ScrollView>
        <StackLayout>
            <StackLayout.Resources>
                <Style TargetType="Entry">
                    <Setter Property="Margin" Value="20, 0" />
                    <Setter Property="FontSize" Value="18" />
                    <Setter Property="WidthRequest" Value="500" />
                    <Setter Property="HorizontalOptions" Value="Center" />
                </Style>

                <Style TargetType="Label">
                    <Setter Property="Margin" Value="20, 20, 20, 0" />
                    <Setter Property="FontSize" Value="Large" />
                    <Setter Property="WidthRequest" Value="500" />
                    <Setter Property="HorizontalOptions" Value="Center" />
                </Style>
            </StackLayout.Resources>
            
            <Label  Text="Телефон:"
               HorizontalOptions="Center" />
            <Entry x:Name="EnPhone" Placeholder="" Keyboard="Telephone" />
            <Label  Text="E-mail:"
               HorizontalOptions="Center" />
            <Entry x:Name="EnMail"  Placeholder="" Keyboard="Email" />
            <Label Text="Пароль: " />
            <Entry x:Name="EnPass"  IsPassword="true" />
            <Label Text="Фамилия:"
               HorizontalOptions="Center" />
            <Entry x:Name="EnSurname"  Placeholder="" />
            <Label  Text="Имя:"
               HorizontalOptions="Center" />
            <Entry x:Name="EnName"  Placeholder="" />
            <Label Text="Отчество:"
               HorizontalOptions="Center" />
            <Entry x:Name="EnOtch"  Placeholder="" />
            <Label  Text="Фирма:"
               HorizontalOptions="Center" />
            <Entry x:Name="EnFirma"  Placeholder="" />
            <Button Text="Зарегистрироваться" x:Name="RegBut" Clicked="Registration_Clicked" HorizontalOptions="Center" VerticalOptions="Center"
                Margin="20, 20, 20 ,0"
                WidthRequest="500" />
        </StackLayout>
    </ScrollView>
</ContentPage>